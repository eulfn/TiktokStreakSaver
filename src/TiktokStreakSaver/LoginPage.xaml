<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TiktokStreakSaver.LoginPage"
             Title="Login to TikTok"
             BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource OffBlack}}">
    
    <Grid RowDefinitions="Auto, *, Auto">
        
        <!-- Header -->
        <Border Grid.Row="0" Margin="16,16,16,8" Style="{StaticResource SectionCard}">
            <VerticalStackLayout Spacing="4">
                <Label Text="TikTok Authentication" FontSize="18" FontAttributes="Bold" TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}"/>
                <Label Text="Login on the official website below to maintain background messaging session. Your credentials are only handled by TikTok directly." FontSize="12" TextColor="{StaticResource Gray500}" LineBreakMode="WordWrap"/>
            </VerticalStackLayout>
        </Border>
        
        <!-- WebView Card -->
        <Border Grid.Row="1" Margin="16,8" Style="{StaticResource SectionCard}" Padding="0" ClipBounds="True">
            <Grid>
                <WebView x:Name="TikTokWebView" Navigated="OnWebViewNavigated"/>
                
                <!-- Loading Overlay -->
                <Grid x:Name="LoadingOverlay" IsVisible="True" BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource CardBackgroundDark}}">
                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="16">
                        <ActivityIndicator IsRunning="True" Color="{StaticResource Primary}" HeightRequest="48" WidthRequest="48"/>
                        <Label Text="Establishing secure connection" FontSize="14" TextColor="{StaticResource Gray500}" HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </Grid>
            </Grid>
        </Border>
        
        <!-- Footer Actions -->
        <Border Grid.Row="2" 
                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}"
                Stroke="{AppThemeBinding Light={StaticResource BorderColorLight}, Dark={StaticResource BorderColorDark}}"
                StrokeThickness="1"
                StrokeShape="Rectangle">
            <Grid ColumnDefinitions="*, *" ColumnSpacing="16" Padding="20,16,20,32">
                <Button Text="Dismiss" Style="{StaticResource TextButton}" Clicked="OnBackClicked" HeightRequest="52"/>
                <Button Grid.Column="1" Text="Reload Page" 
                        BackgroundColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource Gray200}}" 
                        TextColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" 
                        CornerRadius="10" HeightRequest="52" Clicked="OnRefreshClicked" FontAttributes="Bold"/>
            </Grid>
        </Border>
    </Grid>
</ContentPage>
